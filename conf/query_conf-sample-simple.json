{
  "queryModels": [
    {
      "name": "stage",
      "params": {
        "required": ["byo"]
      },
      "query" : {
        "sql": "SELECT stage, COUNT(*) FROM re_%byo_front.re_fsbo GROUP BY stage"
      },
      "data": {
        "title": "column.0",
        "dataset": [
          {
            "name": "# of Ads",
            "from": "column.1"
          }
        ]
      }
    }
  ]
}